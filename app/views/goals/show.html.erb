<h1 class='heading-1'><%= @goal.name %></h1>

<p class='paragraph'>Reflect on what you've sown gently.</p>
<p class='paragraph'>When you're ready, return to the Garden.</p>

<div class='flex gap-8 mt-8'>
  <%= link_to 'Return to the Garden', goals_path, class: 'btn-secondary' %>
  <%= link_to 'Edit', edit_goal_path(@goal), class: 'btn-secondary' %>
  <%= link_to 'Delete', goal_path(@goal), data: {
    turbo_method: :delete,
    turbo_confirm: "Do you want to delete this seed?"
  }, class: 'btn-secondary' %>
</div>
